[project]
name = "proyecto"
version = "0.1.0"
description = "Add your description here"
requires-python = "==3.13.*"
dependencies = [
    "androguard>=4.1.3",
    "django>=5.2",
    "numpy>=2.2",
    "pillow>=11.0",
    "torch>=2.2",
    "torchvision>=0.17",
    "scikit-learn>=1.5",
    "pandas>=2.2",
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "apk_detector.settings"
pythonpath = ["app"]
testpaths  = ["app/pipeline/tests"]
